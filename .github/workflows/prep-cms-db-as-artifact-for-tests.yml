name: Prep Test DB Artifact (Reusable)

on:
  workflow_call:
    inputs:
      servd_project_slug:
        description: 'Servd project slug'
        required: true
        type: string
      servd_environment:
        description: 'Servd environment'
        required: false
        type: string
        default: 'production'
    secrets:
      servd_security_key:
        description: 'Servd security key'
        required: true
      workflow_token:
        description: 'Token for repo checkout and GitHub API calls'
        required: true

jobs:
  dump-database:
    runs-on: ubuntu-latest

    permissions:
      actions: write

    env:
      SERVD_ENVIRONMENT: ${{ inputs.servd_environment }}
      SERVD_PROJECT_SLUG: ${{ inputs.servd_project_slug }}
      SERVD_SECURITY_KEY: ${{ secrets.servd_security_key }}

    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.workflow_token }}
          repository: ${{ github.event.repository.full_name }}

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Enable remote database connectivity
        run: |
          echo "Enabling database access on Servd..."

          RESPONSE=$(curl -s -X POST https://app.servd.host/enable-remote-database-connectivity \
            -H "Content-Type: application/json" \
            -d "{\"slug\":\"$SERVD_PROJECT_SLUG\",\"key\":\"$SERVD_SECURITY_KEY\",\"environment\":\"$SERVD_ENVIRONMENT\"}")

          # Save response to a file for parsing
          echo "$RESPONSE" > response.json

          # Check if response contains an error
          if [[ $(echo "$RESPONSE" | jq -r '.status') != "success" ]]; then
            echo "Error enabling database access:"
            echo "$RESPONSE" | jq -r '.message'
            exit 1
          fi

          # Store connection details
          echo "DB_HOST=$(echo "$RESPONSE" | jq -r '.host')" >> $GITHUB_ENV
          echo "DB_PORT=$(echo "$RESPONSE" | jq -r '.port')" >> $GITHUB_ENV
          echo "DB_USER=$(echo "$RESPONSE" | jq -r '.user')" >> $GITHUB_ENV
          echo "DB_PASSWORD=$(echo "$RESPONSE" | jq -r '.password')" >> $GITHUB_ENV
          echo "DB_NAME=$(echo "$RESPONSE" | jq -r '.database')" >> $GITHUB_ENV
      - name: Install MySQL client
        run: sudo apt-get update && sudo apt-get install -y default-mysql-client

      - name: Dump database
        env:
          TOKEN: ${{ secrets.workflow_token }}
        run: |
          echo "workflow_token length: ${#TOKEN}"
          echo "Creating database dump..."

          # Echo connection details for debugging
            echo "DB_HOST: $DB_HOST"
            echo "DB_PORT: $DB_PORT"
            echo "DB_USER: $DB_USER"
            echo "DB_NAME: $DB_NAME"

          COMMON_OPTS="--no-tablespaces --quick --single-transaction --compression-algorithms=zlib --skip-add-drop-table -h $DB_HOST --port $DB_PORT -u $DB_USER -p$DB_PASSWORD"
          DATA_ONLY_TABLES="contactform_submissions deprecationerrors feedme_logs searchindex sessions tokens webauthn"
          IGNORE_OPTS=""
          for table in $DATA_ONLY_TABLES; do
            IGNORE_OPTS="$IGNORE_OPTS --ignore-table=$DB_NAME.$table"
          done

          MAX_RETRIES=3
          RETRY_DELAY=10
          ATTEMPT=1

          while [ $ATTEMPT -le $MAX_RETRIES ]; do
            echo "Attempt $ATTEMPT of $MAX_RETRIES..."
            
            if mysqldump $COMMON_OPTS $IGNORE_OPTS "$DB_NAME" > browser-tests-db.sql && \
               mysqldump $COMMON_OPTS --no-data "$DB_NAME" $DATA_ONLY_TABLES >> browser-tests-db.sql; then
              echo "Database dump completed successfully"
              break
            else
              EXIT_CODE=$?
              echo "Database dump failed with exit code $EXIT_CODE"
              
              if [ $ATTEMPT -lt $MAX_RETRIES ]; then
                echo "Waiting $RETRY_DELAY seconds before retry..."
                sleep $RETRY_DELAY
                ATTEMPT=$((ATTEMPT + 1))
                rm -f browser-tests-db.sql
              else
                echo "All retry attempts exhausted"
                exit $EXIT_CODE
              fi
            fi
          done

      - name: Upload database dump as artifact
        uses: actions/upload-artifact@v7
        with:
          name: browser-tests-db
          path: browser-tests-db.sql
          retention-days: 7
          overwrite: true

      - name: Delete old browser-tests-db artifacts
        env:
          GH_TOKEN: ${{ secrets.workflow_token }}
        run: |
          set -e
          echo "Fetching all artifacts named browser-tests-db..."

          # --jq '.artifacts[]' streams one object per line across all pages;
          # jq -s '.' collects them into a single flat array.
          ARTIFACTS=$(gh api \
            --paginate \
            "/repos/${{ github.repository }}/actions/artifacts?name=browser-tests-db&per_page=100" \
            --jq '.artifacts[]' \
            | jq -s '.')

          LATEST_ID=$(echo "$ARTIFACTS" | jq '[.[] | select(.expired == false)] | sort_by(.created_at) | last | .id')

          if [ "$LATEST_ID" = "null" ] || [ -z "$LATEST_ID" ]; then
            echo "No non-expired artifacts found; nothing to clean up."
            exit 0
          fi

          echo "Latest artifact ID (will be kept): $LATEST_ID"

          FAILED=0
          while IFS= read -r OLD_ID; do
            echo "Deleting artifact $OLD_ID..."
            if ! gh api --method DELETE "/repos/${{ github.repository }}/actions/artifacts/$OLD_ID"; then
              echo "Warning: failed to delete artifact $OLD_ID"
              FAILED=1
            fi
          done < <(echo "$ARTIFACTS" | jq -r \
            --argjson latest "$LATEST_ID" \
            '.[] | select(.expired == false and .id != $latest) | .id')

          if [ "$FAILED" -ne 0 ]; then
            echo "One or more artifacts could not be deleted."
            exit 1
          fi

          echo "Cleanup complete."

      - name: Disable remote database connectivity
        run: |
          echo "Closing remote database access on Servd..."

          RESPONSE=$(curl -s -X POST https://app.servd.host/disable-remote-database-connectivity \
            -H "Content-Type: application/json" \
            -d "{\"slug\":\"$SERVD_PROJECT_SLUG\",\"key\":\"$SERVD_SECURITY_KEY\"}")
          echo "Response: $RESPONSE"
